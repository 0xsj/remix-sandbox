apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: observability

resources:
  - namespace.yaml
  - prometheus.yaml
  - grafana.yaml
  - kibana.yaml

commonLabels:
  app.kubernetes.io/part-of: n8n-stack
  app.kubernetes.io/managed-by: kustomize
